DATABASE_URL="postgresql://aivy:aivy@localhost:5432/aivy?schema=public"
# Chave usada para assinar o cookie de sessão (JWT). Gere uma própria em
# produção, ex: `node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"`.
AUTH_SECRET="troque-esta-chave-em-producao"
