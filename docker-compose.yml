services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: aivy
      POSTGRES_PASSWORD: aivy
      POSTGRES_DB: aivy
    ports:
      - "5432:5432"
    volumes:
      - aivy_postgres_data:/var/lib/postgresql/data

volumes:
  aivy_postgres_data:
